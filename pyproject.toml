[project]
name = "trueorder"
version = "0.1.0"
description = "Tax-and-fee-adjusted optimal debt repayment sequencing engine for Indian mixed debt portfolios"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
